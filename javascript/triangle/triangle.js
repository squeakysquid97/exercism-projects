//
// This is only a SKELETON file for the 'Triangle' exercise. It's been provided as a
// convenience to get you started writing code faster.
//

export class Triangle {
  constructor(...sides) {
    
  }

  get isEquilateral() {
    if(this.sides === this.sides) return true
    else if(this.sides != this.sides) return false
    else if(this.sides === 0) return false
    else return false
  }

  get isIsosceles() {

  }

  get isScalene() {
    if(this.sides != this.sides) {
      return true
    }else {
      return false
    }
  }
}
